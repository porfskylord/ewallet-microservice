# Stop containers
docker-compose down -v
# Start containers
docker compose up -d

docker ps

# postgres access
docker exec -it ewallet-microservices-postgres-1 psql -U ewallet_admin -d ewallet



# radis access
docker exec -it ewallet-microservice-redis-1 redis-cli
MONITOR

# kafka access
docker exec -it ewallet-microservice-kafka-1 kafka-topics --list --bootstrap-server localhost:9092

docker exec -it ewallet-microservice-kafka-1 kafka-topics --create --topic test-topic --bootstrap-server localhost:9092 --partitions 1 --replication-factor 1

docker exec -it ewallet-microservice-kafka-1 kafka-console-producer --topic test-topic --bootstrap-server localhost:9092

docker exec -it ewallet-microservice-kafka-1 kafka-console-consumer --topic test-topic --bootstrap-server localhost:9092 --from-beginning



# keycloak access
http://localhost:8085

# mvn commands
mvn clean install -DskipTests
mvn spring-boot:run

# git commands
# git init
# git add .
# git commit -m "Initial commit"
# git branch -M main
# git remote add origin https://github.com/your-username/your-repo.git
# git push -u origin main

for bash scripts safety
# git config --global core.autocrlf input
# git add --renormalize .
